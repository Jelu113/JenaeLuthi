// import ProjectList from '../ProjectList';

// export default function ListPage(props) {
//     const {id, title, image,projectDetails} = props
//     return (
//         ProjectList.props
 
    // id={props.id}
    // title={props.title}
    // image={props.image}
    // projectDescription={props.projectDescription}

//     )
// };